@* @page
@model RegisterConfirmationModel
@{
    ViewData["Title"] = "Register confirmation";
}

<h1>@ViewData["Title"]</h1>
@{
    if (@Model.DisplayConfirmAccountLink)
    {
<p>
    This app does not currently have a real email sender registered, see <a href="https://aka.ms/aspaccountconf">these docs</a> for how to configure a real email sender.
    Normally this would be emailed: <a id="confirm-link" href="@Model.EmailConfirmationUrl">Click here to confirm your account</a>
</p>
    }
    else
    {
<p>
        Please check your email to confirm your account.
</p>
    }
} *@

@page
@model RegisterConfirmationModel

@{
    ViewData["Title"] = "RegisterConfirmation";
}

<div class="container py-4">
    <h2 class="mb-2">@ViewData["Title"]</h2>
    <p class="text-muted">Chúng tôi đã gửi email xác nhận tới <strong>@Model.Email</strong>.</p>

    <div class="alert alert-info">
        Vui lòng mở hộp thư và nhấp vào liên kết để kích hoạt tài khoản.
        <br />
        Mẹo: kiểm tra cả thư mục <b>Spam/Junk</b> nếu không thấy trong Inbox.
    </div>

    <a class="btn btn-primary" asp-page="/Account/Login">Quay về trang đăng nhập</a>

    @* (Tuỳ chọn) Hiện thêm link gửi lại *@
    @* <div class="mt-3">
       Bạn chưa nhận được email?
        <a asp-controller="Authentication" asp-action="ResendConfirmation" asp-route-email="@Model.Email">Gửi lại</a>
    </div> *@
</div>
